import { html } from 'lit-html';
import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import 'Components/buttons/base-button.js';

<Meta
  title='Buttons/BaseButton'
  component='base-button'
  args={{
    text: 'Click Me!',
    containerWidth: '200px',
  }}
  argTypes={{
    text: { 
      name: 'Text',
      control: { type: 'text' } 
    },
    containerWidth: { 
      name: 'Container Width',
      control: { type: 'text' } 
    },
  }}
/>

export const Template = (args) => {
  return html`
    <div style="width: ${args.containerWidth}">
      <base-button text=${args.text}></base-button>
    </div>
  `;
}

# Base Button

This is not to be used directly, but rather as a base upon which other buttons can be built.

<Canvas>
  <Story name='BaseButton'>
    { Template.bind({}) }
  </Story>
</Canvas>

<ArgsTable of='base-button' />