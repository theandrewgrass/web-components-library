import { html } from 'lit-html';
import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import 'Components/buttons/secondary-button/secondary-button.js';

<Meta
  title='Buttons/SecondaryButton'
  component='secondary-button'
  args={{
    text: 'Click Me!',
    containerWidth: '200px',
  }}
  argTypes={{
    text: { 
      name: 'Text',
      control: { type: 'text' } 
    },
    containerWidth: { 
      name: 'Container Width',
      control: { type: 'text' } 
    },
  }}
/>

export const Template = (args) => {
  return html`
    <div style="width: ${args.containerWidth}">
      <secondary-button text="${args.text}"></secondary-button>
    </div>
  `;
}

# Secondary Button

This is a secondary button.

<Canvas>
  <Story name='SecondaryButton'>
    { Template.bind({}) }
  </Story>
</Canvas>

<ArgsTable of='secondary-button' />